# Server-side secrets only. Never put these in Flutter, web/, localStorage, or GitHub Pages.
DATABASE_URL=postgresql://user:password@host:5432/ai_winrate
ODDS_API_KEY=replace_me
WEATHER_API_KEY=replace_me
# Optional provider credential if your MLB provider requires one.
MLB_API_KEY=
# Comma-separated browser origins allowed to call the API.
CORS_ORIGINS=https://ez3220.github.io
